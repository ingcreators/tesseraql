version: tesseraql/v1
id: tql.iamAdmin.groups.revokeRole
kind: route
recipe: command-json

security:
  policy: tql.iam.admin.write

input:
  roleCode:
    type: string
    required: true

steps:
  - id: main
    service:
      name: iam.revokeGroupRole
      params:
        groupCode: path.code
        roleCode: params.roleCode
response:
  redirect:
    location: /_tesseraql/admin/groups/{path.code}?revoked=1
