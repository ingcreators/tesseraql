version: tesseraql/v1
id: tql.iamAdmin.users.revokeEligibility
kind: route
recipe: command-json

security:
  policy: tql.iam.admin.write

input:
  roleCode:
    type: string
    required: true

steps:
  - id: main
    service:
      name: iam.revokeEligibility
      params:
        userId: path.id
        roleCode: params.roleCode
response:
  redirect:
    location: /_tesseraql/admin/users/{path.id}?ineligible=1
