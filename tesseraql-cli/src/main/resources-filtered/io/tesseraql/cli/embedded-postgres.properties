# Filtered at build time: the embedded PostgreSQL binary version the serve embedded-db option
# resolves on demand. Single-sourced from the reactor property zonky.postgres.binaries.version.
version=${zonky.postgres.binaries.version}
