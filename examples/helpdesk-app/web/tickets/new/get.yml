version: tesseraql/v1
id: tickets.new
kind: route
recipe: query-html

security:
  policy: help.read

sources:
  main:
    sql:
      file: none.sql
      mode: query
response:
  html:
    status: 200
    view: tickets.new.form
