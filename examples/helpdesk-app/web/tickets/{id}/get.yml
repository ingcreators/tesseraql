version: tesseraql/v1
id: tickets.detail
kind: route
recipe: query-html

security:
  policy: help.read

sources:
  main:
    sql:
      file: ticket.sql
      mode: query
      params:
        id: path.id
response:
  html:
    status: 200
    view: tickets.detail.view
