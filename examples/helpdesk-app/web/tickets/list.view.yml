version: tesseraql/v1
id: tickets
kind: view
recipe: list
key: id
title: Tickets
search: q
filters: [status, priority]
presets:
  - name: Open tickets
    params: { status: open }
  - name: High priority
    params: { priority: high, sort: "-created_at" }
# The filtered set as a file (docs/list-export.md): the asynchronous export, kicked off from
# the page with the conditions it shows.
exports: [/tickets/export]
columns:
  - { name: id, label: "#", link: "/tickets/{id}" }
  - { name: subject, sortable: true }
  - { name: priority, sortable: true }
  - { name: requester }
  - { name: assignee }
  - { name: status, sortable: true }
