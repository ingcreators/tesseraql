tesseraql:
  app:
    name: user-admin
    work: ${TESSERAQL_WORK_HOME:${TESSERAQL_APP_HOME}/work}

  # Phase 22 internationalization: the app serves English and Japanese. The request locale
  # resolves from the ?lang= query parameter (handy for a language toggle), then the signed-in
  # user's locale claim, then Accept-Language, then the default. Catalogs live in
  # messages/<locale>.yml.
  i18n:
    defaultLocale: en
    locales: [en, ja]
    preference:
      - query.lang
      - principal.claim.locale

  datasources:
    main:
      jdbcUrl: ${db.main.url}
      username: ${db.main.username}
      password: ${db.main.password}
      maximumPoolSize: ${db.main.maximumPoolSize:10}

  # Phase 20 notification channels: notify: declarations reference these by name.
  # Credentials would ride the SecretResolver SPI (${secret.env.SMTP_PASSWORD}); the local
  # defaults below point at a dev SMTP server and a dev webhook receiver.
  notifications:
    channels:
      user-mail:
        type: mail
        host: ${MAIL_HOST:localhost}
        port: ${MAIL_PORT:2525}
        from: noreply@example.com
        to: ops@example.com
        subject: "Account provisioned: [(${payload.userName})]"
        template: templates/mail/provisioned.txt
      audit-webhook:
        type: webhook
        url: ${AUDIT_WEBHOOK_URL:http://localhost:9999/hook}
        secret: ${AUDIT_WEBHOOK_SECRET:dev-only-webhook-secret}
    # Operations alerts (job failures, threshold breaches) reuse the same channels:
    # alerts:
    #   channel: audit-webhook

  # Phase 26 managed connectors: outbound HTTP for http-call pipeline steps. Egress is
  # deny-by-default — a call may only target a host in allowedHosts (exact or *.wildcard).
  # Credentials ride the SecretResolver SPI and resolve at call time, never into artifacts.
  http:
    outbound:
      allowedHosts:
        - api.directory.example
        # The audit webhook channel above delivers through this same gateway, so its host is
        # allow-listed here too — egress is deny-by-default for the framework's own calls.
        - localhost
      credentials:
        directory:
          type: header
          header: X-API-Key
          value: ${secret.env.DIRECTORY_API_KEY:dev-only-directory-key}

  identity:
    defaultRealm: local
    realms:
      local:
        type: managed
        datasource: main

  security:
    defaults:
      routes:
        - match: /api/**
          auth: bearer
        - match: /**
          auth: browser
          csrf: auto

    # The security header block every HTML response sends (docs/response-shaping.md
    # "Default response headers"); a route overrides per header name, `unset` suppresses.
    responseHeaders:
      Content-Security-Policy: "default-src 'self'; style-src 'self' 'unsafe-inline'; frame-ancestors 'none'"
      X-Content-Type-Options: nosniff
      X-Frame-Options: DENY
      Referrer-Policy: no-referrer

    jwt:
      secret: ${JWT_SECRET:dev-only-secret-change-me-in-production}
      # Which tokens are for this application. Without it, any token the issuer
      # minted for any other relying party would be accepted (TQL-SEC-4048).
      audience:
        - https://user-admin.example.com
      rolesClaim: roles
      permissionsClaim: permissions
      tenantClaim: tenant_id

    policies:
      # The starter policies every scaffolded route references (tesseraql new ships the same
      # block): defining them here keeps "scaffold CRUD from a table" servable out of the box.
      # The example's ADMIN operator may browse scaffolded slices too. Permission codes carry
      # the application's own name as their first segment (TQL-YAML-1406) so no two
      # applications silently share one grant; roles stay the deployment's vocabulary.
      app.read:
        anyOf:
          - role: APP_READ
          - role: ADMIN
          - permission: user-admin.read
      app.write:
        anyOf:
          - role: APP_WRITE
          - role: ADMIN
          - permission: user-admin.write
      users.read:
        anyOf:
          - role: USER_READ
          - permission: user-admin.users.read
      users.write:
        anyOf:
          - role: USER_WRITE
          - permission: user-admin.users.write
      # IAM Admin and the operations console are framework surfaces and check their own
      # granted atoms directly (tql.iam.admin.view/.write, tql.ops.view.<name> /
      # tql.ops.run.<name>, docs/stack-shells.md) — no framework policies here anymore.
