version: tesseraql/v1
id: tql.identity.users.list
kind: route
recipe: query-html

security:
  auth: browser
  policy: users.read

sql:
  contract: identity.list-users

response:
  html:
    status: 200
    template: table.html
    model:
      users: sql.rows
      count: sql.rowCount
