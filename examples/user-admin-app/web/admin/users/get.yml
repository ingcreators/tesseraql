version: tesseraql/v1
id: tql.identity.users.list
kind: route
recipe: query-html

security:
  policy: users.read

sources:
  main:
    contract:
      name: identity.list-users

response:
  html:
    status: 200
    template: index.html
    model:
      users: main.rows
      count: main.rowCount
