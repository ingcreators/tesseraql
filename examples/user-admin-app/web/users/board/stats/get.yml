version: tesseraql/v1
id: users.board.stats
kind: route
recipe: query-html

security:
  policy: users.read

sql:
  file: totals.sql

queries:
  byStatus:
    file: by-status.sql
  recent:
    file: recent.sql

response:
  html:
    view: stats.view.yml
