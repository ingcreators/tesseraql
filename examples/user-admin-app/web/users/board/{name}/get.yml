version: tesseraql/v1
id: users.board.detail
kind: route
recipe: query-html

security:
  policy: users.read

sources:
  main:
    sql:
      file: detail.sql
      params:
        name: path.name
  groups:
    sql:
      file: groups.sql
response:
  html:
    view: users.board.detail
