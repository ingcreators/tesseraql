version: tesseraql/v1
id: tql.identity.users.disable
kind: route
recipe: command-json

security:
  policy: users.write

steps:
  - id: main
    contract:
      name: identity.disable-user
      mode: update
      params:
        userId: path.id
response:
  json:
    status: 200
    body:
      affected: steps.main.affectedRows
