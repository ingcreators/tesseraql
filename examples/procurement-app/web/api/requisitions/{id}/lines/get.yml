version: tesseraql/v1
id: api.requisitions.lines
kind: route
recipe: query-json

input:
  id:
    type: string
    required: true
    maxLength: 64

security:
  policy: req.read

sql:
  file: lines.sql
  mode: query
  params:
    id: path.id

response:
  json:
    status: 200
    body:
      data: sql.rows
