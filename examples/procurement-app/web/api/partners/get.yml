version: tesseraql/v1
id: api.partners.list
kind: route
recipe: query-json

security:
  policy: rfq.read

sql:
  file: partners.sql
  mode: query

response:
  json:
    status: 200
    body:
      data: sql.rows
