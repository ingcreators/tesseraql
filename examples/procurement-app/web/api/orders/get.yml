version: tesseraql/v1
id: api.orders.list
kind: route
recipe: query-json

security:
  policy: rfq.read

sql:
  file: orders.sql
  mode: query

response:
  json:
    status: 200
    body:
      data: sql.rows
      meta:
        count: sql.rowCount
