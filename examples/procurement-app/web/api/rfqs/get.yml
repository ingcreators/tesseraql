version: tesseraql/v1
id: api.rfqs.list
kind: route
recipe: query-json

security:
  policy: rfq.read

sql:
  file: rfqs.sql
  mode: query

response:
  json:
    status: 200
    body:
      data: sql.rows
      meta:
        count: sql.rowCount
