version: tesseraql/v1
id: api.rfqs.list
kind: route
recipe: query-json

security:
  policy: rfq.read

sources:
  main:
    sql:
      file: rfqs.sql
      mode: query
response:
  json:
    status: 200
    body:
      data: main.rows
      meta:
        count: main.rowCount
