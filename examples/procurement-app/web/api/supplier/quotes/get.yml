version: tesseraql/v1
id: api.supplier.quotes
kind: route
recipe: query-json

security:
  policy: sup.read

sources:
  main:
    sql:
      file: quotes.sql
      mode: query
response:
  json:
    status: 200
    body:
      data: main.rows
