version: tesseraql/v1
id: api.supplier.quotes
kind: route
recipe: query-json

security:
  policy: sup.read

sql:
  file: quotes.sql
  mode: query

response:
  json:
    status: 200
    body:
      data: sql.rows
