version: tesseraql/v1
id: rfqs.page
kind: route
recipe: query-html

security:
  policy: rfq.read

pagination:
  size: 20
  count: true

sources:
  main:
    sql:
      file: rfqs.sql
      mode: query
response:
  html:
    status: 200
    view: rfqs
