{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tesseraql.io/schema/tesseraql-view-v1.schema.json",
  "title": "TesseraQL view document v1",
  "description": "Schema for TesseraQL declarative view documents (*.view.yml): what a route renders through the framework's tql/view/* patterns instead of a hand-written template. Documented in declarative-views.md.",
  "$comment": "A view document is strict: the loader refuses an unknown key at every nesting level (TQL-VIEW-3314), so this schema declares additionalProperties false to match.",
  "type": "object",
  "required": [
    "version",
    "id",
    "kind"
  ],
  "properties": {
    "version": {
      "$ref": "tesseraql-defs-v1.schema.json#/$defs/shared/version"
    },
    "id": {
      "$ref": "tesseraql-defs-v1.schema.json#/$defs/shared/id"
    },
    "kind": {
      "const": "view",
      "description": "Declarative view documents (the *.view.yml convention)."
    },
    "recipe": {
      "description": "Which view this document is: list | form | detail | dashboard. A form derives its fields from the action: route's input: block; a dashboard composes panels:.",
      "enum": [
        "list",
        "form",
        "detail",
        "dashboard"
      ]
    },
    "title": {
      "type": "string",
      "description": "The heading this view renders. A message key resolves through the app bundles; anything else renders literally."
    },
    "template": {
      "type": "string",
      "description": "The pattern override this view renders through instead of its bundled `tql/view/*` pattern (customization ladder L2). Resolved beside the view document, then under the app template root. Documented in declarative-views.md."
    },
    "action": {
      "type": "string",
      "description": "The id of the command route a form view submits to; its `input:` block is where the form's fields come from."
    },
    "source": {
      "type": "string",
      "description": "The model key a view reads its rows from - a name in the route's `sources:`, whatever arm it declares, defaulting to `main`."
    },
    "search": {
      "type": "string",
      "description": "The route input a list view binds its search box to; it must be declared on the route."
    },
    "fields": {
      "type": "array",
      "description": "The fields a form or detail view renders, in order. Documented in declarative-views.md."
    },
    "columns": {
      "type": "array",
      "description": "The columns a list view renders, in order. Documented in declarative-views.md."
    },
    "children": {
      "type": "object",
      "additionalProperties": true,
      "description": "Views embedded inside this one, keyed by the slot each fills. Documented in declarative-views.md."
    },
    "panels": {
      "type": "array",
      "description": "The panels a dashboard view composes, in order. Documented in declarative-views.md."
    },
    "slots": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Named regions this view exposes for a composing parent to fill. Documented in declarative-views.md."
    },
    "refreshOn": {
      "type": "string",
      "description": "Refetch this view's refresh region whenever a command emits this topic (docs/realtime.md). List, detail and dashboard views only - not forms."
    },
    "layout": {
      "type": "string",
      "enum": ["card", "page"],
      "description": "How a list view frames itself (docs/declarative-views.md): 'card' (the default) renders in the page flow; 'page' renders the operational grid page - fixed chrome, only the grid scrolls, in-place paging. List views only."
    },
    "key": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" }, "minItems": 1 }
      ],
      "description": "The result columns that identify one row (docs/declarative-views.md): a column name, or an ordered list for a composite key. Rows gain a stable anchor and an opaque row token; every key column must be present and non-null in each row. List views only."
    },
    "filters": {
      "type": "array",
      "items": {
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "required": ["name"],
            "properties": {
              "name": { "type": "string", "description": "The declared route input this filter binds." },
              "label": { "type": "string", "description": "The heading the condition chip and dialog field carry; a message key resolves through the catalog." }
            },
            "additionalProperties": false
          }
        ]
      },
      "description": "The grid page's declared filters (docs/declarative-views.md): route inputs rendered as condition chips and a filter dialog. Each entry is an input name, or a name/label mapping. Requires layout: page; list views only."
    },
    "presets": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "params"],
        "properties": {
          "name": { "type": "string", "description": "The link's label; a message key resolves through the catalog." },
          "params": {
            "type": "object",
            "additionalProperties": { "type": ["string", "number", "boolean"] },
            "description": "The query state the link applies - declared route inputs plus the framework sort/dir/size params."
          }
        },
        "additionalProperties": false
      },
      "description": "Named view presets (docs/declarative-views.md): contract-declared param sets the grid page renders as real links - the active one is marked, re-clicking it resets, and no storage is involved. Requires layout: page; list views only."
    },
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["label", "action"],
        "properties": {
          "label": { "type": "string", "description": "The button's label; a message key resolves through the catalog." },
          "action": { "type": "string", "description": "The POST route the selection submits the checked rows' tokens to (repeated ids fields)." },
          "confirm": { "type": "string", "description": "A confirmation prompt gating the action (the kit's confirm dialog)." }
        },
        "additionalProperties": false
      },
      "description": "Bulk actions over the grid page's row selection (docs/declarative-views.md): declaring any renders the selection column and bar. Requires layout: page and key:; list views only."
    }
  },
  "additionalProperties": false
}
