# tesseraql-scaffold-checksum: sha256:f4cfdf72c6b60580d8b36301e3d1e9f16e997cf6ffb8403b7710b6c3b8aebf1f
# Scaffolded update command for the items table: optimistic locking turns a stale edit into 409 Conflict.
version: tesseraql/v1
id: items.update
kind: route
recipe: command-json

input:
  id:
    domain: items.id
    required: true
  name:
    domain: items.name
    required: true
  quantity:
    domain: items.quantity
    required: true
  unitPrice:
    domain: items.unitPrice
  dueDate:
    domain: items.dueDate
  active:
    domain: items.active
    required: true
  note:
    domain: items.note
  version:
    domain: items.version
    required: true

inputPolicy:
  unknownFields: reject

security:
  policy: app.write

validate:
  nameIsFree:
    use: itemsNameIsFree
    params:
      name: params.name
      excludeId: params.id
    field: name

sql:
  file: update.sql
  mode: update
  expect:
    rowCount: 1
    onMismatch: conflict
  params:
    id: params.id
    name: params.name
    quantity: params.quantity
    unitPrice: params.unitPrice
    dueDate: params.dueDate
    active: params.active
    note: params.note
    version: params.version

response:
  redirect:
    location: /items/{path.id}
