version: tesseraql/v1
id: tql.studio.security
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.security
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: security.html
    model:
      sec: sql
