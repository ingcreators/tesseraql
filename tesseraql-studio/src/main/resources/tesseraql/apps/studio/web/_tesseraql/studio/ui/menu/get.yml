version: tesseraql/v1
id: tql.studio.menu
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.menu.view
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: menu.html
    model:
      menu: sql
