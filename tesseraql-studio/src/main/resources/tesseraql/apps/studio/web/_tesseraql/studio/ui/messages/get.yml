version: tesseraql/v1
id: tql.studio.messages
kind: route
recipe: query-html

input:
  saved:
    type: string
    required: false

sql:
  service: studio.messages
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: messages.html
    model:
      msg: sql
      saved: params.saved
