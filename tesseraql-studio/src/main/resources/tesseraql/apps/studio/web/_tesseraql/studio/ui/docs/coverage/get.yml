version: tesseraql/v1
id: tql.studio.docs.coverage
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: docs.coverage

response:
  html:
    status: 200
    template: coverage.html
    model:
      cov: sql
