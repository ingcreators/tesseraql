version: tesseraql/v1
id: tql.studio.jobs
kind: route
recipe: query-html

input:
  name:
    type: string
    required: false
  saved:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.jobs.view
      params:
        name: params.name
        saved: params.saved
        permissions: principal.permissions
response:
  html:
    status: 200
    template: jobs.html
    model:
      jp: main
