version: tesseraql/v1
id: tql.studio.docs.domains
kind: route
recipe: query-html

input:
  Cookie:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.docs.domains
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: params.Cookie

response:
  html:
    status: 200
    template: domains.html
    model:
      dom: main
