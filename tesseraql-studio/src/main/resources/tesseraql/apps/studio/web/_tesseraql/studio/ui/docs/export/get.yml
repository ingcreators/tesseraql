version: tesseraql/v1
id: tql.studio.docs.export
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: docs.export

response:
  html:
    status: 200
    template: export.html
    model:
      docs: sql
