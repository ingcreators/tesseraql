version: tesseraql/v1
id: tql.studio.explorer
kind: route
recipe: query-html

input:
  q:
    type: string
    required: false

security:
  auth: browser

sql:
  service: studio.explorer
  params:
    q: params.q
    roles: principal.roles

response:
  html:
    status: 200
    template: explorer.html
    model:
      studio: sql
