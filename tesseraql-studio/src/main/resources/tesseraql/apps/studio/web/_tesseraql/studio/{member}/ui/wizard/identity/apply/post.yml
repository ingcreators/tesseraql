version: tesseraql/v1
id: tql.studio.wizard.identity.apply
kind: route
recipe: query-html

input:
  _csrf:
    type: string
    required: false
  realmId:
    type: string
    required: true
  type:
    type: string
    required: true
  datasource:
    type: string
    required: true
  sqlRoot:
    type: string
    required: false
  userManagement:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.wizard.identity.apply
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie
        shellCsrf: params._csrf
        shellCsrfHeader: header.X-CSRF-Token
        realmId: params.realmId
        type: params.type
        datasource: params.datasource
        sqlRoot: params.sqlRoot
        userManagement: params.userManagement
        permissions: principal.permissions
        actor: principal.loginId
response:
  html:
    status: 200
    template: applied.html
    model:
      result: main
