version: tesseraql/v1
id: tql.studio.sqlBuilder.columns
kind: route
recipe: query-html

input:
  table:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.sqlBuilder.columns
      params:
        table: params.table
response:
  html:
    status: 200
    template: columns.html
    model:
      cols: main
