version: tesseraql/v1
id: tql.studio.docs.search
kind: route
recipe: query-html

input:
  q:
    type: string
    required: false

sources:
  main:
    service:
      name: docs.search
      params:
        q: query.q
response:
  html:
    status: 200
    template: results.html
    model:
      results: main
