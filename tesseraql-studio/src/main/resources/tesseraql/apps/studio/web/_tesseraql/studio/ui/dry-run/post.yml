version: tesseraql/v1
id: tql.studio.migration.dryRun
kind: route
recipe: query-html

input:
  path:
    type: string
    required: true
  content:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.migration.dryRun
      params:
        path: params.path
        content: params.content
response:
  html:
    status: 200
    template: dry-run.html
    model:
      dryRun: main
