version: tesseraql/v1
id: tql.studio.migration
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.migration.new
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: migration.html
    model:
      mig: sql
