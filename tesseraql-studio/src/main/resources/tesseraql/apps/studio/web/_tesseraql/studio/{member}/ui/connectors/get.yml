version: tesseraql/v1
id: tql.studio.connectors
kind: route
recipe: query-html

input:
  Cookie:
    type: string
    required: false
  saved:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.connectors.view
      params:
        member: path.member
        shellCookie: params.Cookie
        saved: params.saved
        shellPermissions: principal.permissions
response:
  html:
    status: 200
    template: connectors.html
    model:
      cx: main
