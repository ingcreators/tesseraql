version: tesseraql/v1
id: tql.studio.sqlBuilder.columns
kind: route
recipe: query-html

input:
  table:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.sqlBuilder.columns
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie
        table: params.table
response:
  html:
    status: 200
    template: columns.html
    model:
      cols: main
