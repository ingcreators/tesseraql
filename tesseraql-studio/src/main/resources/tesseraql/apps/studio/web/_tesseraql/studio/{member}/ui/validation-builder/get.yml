version: tesseraql/v1
id: tql.studio.validationBuilder
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.shell.studio.validationBuilder
      params:
        member: path.member
        shellCookie: header.Cookie
        shellPermissions: principal.permissions
response:
  html:
    status: 200
    template: validation-builder.html
    model:
      vb: main
