version: tesseraql/v1
id: tql.studio.scaffold.apply
kind: route
recipe: query-html

input:
  _csrf:
    type: string
    required: false
  table:
    type: string
    required: true
  force:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.scaffold.apply
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie
        shellCsrf: params._csrf
        shellCsrfHeader: header.X-CSRF-Token
        table: params.table
        force: params.force
        actor: principal.loginId
        permissions: principal.permissions
response:
  html:
    status: 200
    template: apply.html
    model:
      scaffold: main
