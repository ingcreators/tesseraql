version: tesseraql/v1
id: tql.studio.docs.decisions
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.shell.docs.decisions
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie

response:
  html:
    status: 200
    template: decisions.html
    model:
      dom: main
