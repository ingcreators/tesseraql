version: tesseraql/v1
id: tql.studio.runTests
kind: route
recipe: query-html

input:
  path:
    type: string
    required: true

sources:
  main:
    service:
      name: studio.runTests
      params:
        path: params.path
response:
  html:
    status: 200
    template: run-tests.html
    model:
      tests: main
