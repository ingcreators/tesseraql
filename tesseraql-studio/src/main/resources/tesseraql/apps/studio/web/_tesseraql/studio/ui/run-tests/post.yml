version: tesseraql/v1
id: tql.studio.runTests
kind: route
recipe: query-html

input:
  path:
    type: string
    required: true

security:
  auth: browser

sql:
  service: studio.runTests
  params:
    path: params.path

response:
  html:
    status: 200
    template: run-tests.html
    model:
      tests: sql
