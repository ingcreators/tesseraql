version: tesseraql/v1
id: tql.studio.tryit
kind: route
recipe: query-html

input:
  path:
    type: string
    required: false
  method:
    type: string
    required: false

security:
  auth: browser

sql:
  service: studio.tryit
  params:
    path: params.path
    method: params.method

response:
  html:
    status: 200
    template: try.html
    model:
      tryit: sql
