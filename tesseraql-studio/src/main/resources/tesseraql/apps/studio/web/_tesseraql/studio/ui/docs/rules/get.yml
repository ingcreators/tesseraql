version: tesseraql/v1
id: tql.studio.docs.rules
kind: route
recipe: query-html

sources:
  main:
    service:
      name: docs.rules

response:
  html:
    status: 200
    template: rules.html
    model:
      dom: sql
