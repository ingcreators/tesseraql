version: tesseraql/v1
id: tql.studio.health
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.health

response:
  html:
    status: 200
    template: health.html
    model:
      health: sql
