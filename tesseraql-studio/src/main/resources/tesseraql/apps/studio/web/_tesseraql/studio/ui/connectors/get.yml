version: tesseraql/v1
id: tql.studio.connectors
kind: route
recipe: query-html

input:
  saved:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.connectors.view
      params:
        saved: params.saved
        permissions: principal.permissions
response:
  html:
    status: 200
    template: connectors.html
    model:
      cx: main
