version: tesseraql/v1
id: tql.studio.connectors
kind: route
recipe: query-html

input:
  saved:
    type: string
    required: false

security:
  auth: browser

sql:
  service: studio.connectors.view
  params:
    saved: params.saved
    roles: principal.roles

response:
  html:
    status: 200
    template: connectors.html
    model:
      cx: sql
