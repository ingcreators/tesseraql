version: tesseraql/v1
id: tql.studio.sqlBuilder
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.sqlBuilder.new
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: sql-builder.html
    model:
      b: sql
