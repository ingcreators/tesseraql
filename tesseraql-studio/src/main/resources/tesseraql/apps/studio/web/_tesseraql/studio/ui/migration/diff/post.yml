version: tesseraql/v1
id: tql.studio.migration.diff
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.migration.diff

response:
  html:
    status: 200
    template: diff.html
    model:
      build: main
