version: tesseraql/v1
id: tql.studio.migration.diff
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.migration.diff

response:
  html:
    status: 200
    template: diff.html
    model:
      build: sql
