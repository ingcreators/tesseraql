version: tesseraql/v1
id: tql.studio.mail
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.shell.studio.mail
      params:
        member: path.member
        shellCookie: header.Cookie
        shellPermissions: principal.permissions
response:
  html:
    status: 200
    template: mail.html
    model:
      mail: main
