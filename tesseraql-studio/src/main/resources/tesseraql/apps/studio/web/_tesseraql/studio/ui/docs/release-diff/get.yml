version: tesseraql/v1
id: tql.docs.releaseDiff
kind: route
recipe: query-html

input:
  captured:
    type: string
    required: false

sql:
  service: docs.releaseDiff
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: release-diff.html
    model:
      docs: sql
      captured: params.captured
