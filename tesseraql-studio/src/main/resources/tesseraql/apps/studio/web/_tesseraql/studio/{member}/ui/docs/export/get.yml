version: tesseraql/v1
id: tql.studio.docs.export
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.shell.docs.export
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie

response:
  html:
    status: 200
    template: export.html
    model:
      docs: main
