version: tesseraql/v1
id: tql.studio.copilot
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.copilot.view
  params:
    roles: principal.roles
    actor: principal.loginId

response:
  html:
    status: 200
    template: copilot.html
    model:
      chat: sql
