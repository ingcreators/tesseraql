version: tesseraql/v1
id: tql.studio.copilot
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.copilot.view
      params:
        permissions: principal.permissions
        actor: principal.loginId
response:
  html:
    status: 200
    template: copilot.html
    model:
      chat: main
