version: tesseraql/v1
id: tql.studio.sqlBuilder
kind: route
recipe: query-html

input:
  Cookie:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.sqlBuilder.new
      params:
        member: path.member
        shellCookie: params.Cookie
        shellPermissions: principal.permissions
response:
  html:
    status: 200
    template: sql-builder.html
    model:
      b: main
