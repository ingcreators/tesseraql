version: tesseraql/v1
id: tql.studio.docs.route
kind: route
recipe: query-html

input:
  id:
    type: string
    required: true

security:
  auth: browser

sql:
  service: docs.route
  params:
    id: query.id

response:
  html:
    status: 200
    template: route.html
    model:
      doc: sql
