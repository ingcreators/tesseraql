version: tesseraql/v1
id: tql.studio.save
kind: route
recipe: command-json

input:
  _csrf:
    type: string
    required: false
  path:
    type: string
    required: true
  content:
    type: string
    required: false

steps:
  - id: main
    service:
      name: studio.shell.studio.save
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie
        shellCsrf: params._csrf
        shellCsrfHeader: header.X-CSRF-Token
        path: params.path
        content: params.content
        permissions: principal.permissions
response:
  redirect:
    location: /_tesseraql/studio/ui/source?path={params.path}&saved=1
