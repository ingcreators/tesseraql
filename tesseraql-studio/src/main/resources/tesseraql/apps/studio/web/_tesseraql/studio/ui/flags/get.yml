version: tesseraql/v1
id: tql.studio.flags
kind: route
recipe: query-html

input:
  saved:
    type: string
    required: false
  removed:
    type: string
    required: false

sql:
  service: studio.flags
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: flags.html
    model:
      f: sql
      saved: params.saved
      removed: params.removed
