version: tesseraql/v1
id: tql.studio.flags
kind: route
recipe: query-html

security:
  auth: browser

sql:
  service: studio.flags
  params:
    roles: principal.roles

response:
  html:
    status: 200
    template: flags.html
    model:
      f: sql
