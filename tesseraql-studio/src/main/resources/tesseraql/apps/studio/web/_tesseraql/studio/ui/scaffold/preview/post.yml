version: tesseraql/v1
id: tql.studio.scaffold.preview
kind: route
recipe: query-html

input:
  table:
    type: string
    required: true

sources:
  main:
    service:
      name: studio.scaffold.preview
      params:
        table: params.table
response:
  html:
    status: 200
    template: preview.html
    model:
      scaffold: main
