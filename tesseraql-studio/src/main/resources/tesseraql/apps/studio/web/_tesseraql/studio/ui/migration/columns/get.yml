version: tesseraql/v1
id: tql.studio.migration.columns
kind: route
recipe: query-html

input:
  table:
    type: string
    required: false

security:
  auth: browser

sql:
  service: studio.migration.columns
  params:
    table: params.table

response:
  html:
    status: 200
    template: columns.html
    model:
      cols: sql
