version: tesseraql/v1
id: tql.studio.migration.columns
kind: route
recipe: query-html

input:
  table:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.migration.columns
      params:
        table: params.table
response:
  html:
    status: 200
    template: columns.html
    model:
      cols: main
