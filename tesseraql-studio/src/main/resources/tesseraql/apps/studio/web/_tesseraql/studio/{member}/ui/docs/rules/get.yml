version: tesseraql/v1
id: tql.studio.docs.rules
kind: route
recipe: query-html

sources:
  main:
    service:
      name: studio.shell.docs.rules
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie

response:
  html:
    status: 200
    template: rules.html
    model:
      dom: main
