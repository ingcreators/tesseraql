version: tesseraql/v1
id: tql.studio.migration.columns
kind: route
recipe: query-html

input:
  Cookie:
    type: string
    required: false
  table:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.migration.columns
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: params.Cookie
        table: params.table
response:
  html:
    status: 200
    template: columns.html
    model:
      cols: main
