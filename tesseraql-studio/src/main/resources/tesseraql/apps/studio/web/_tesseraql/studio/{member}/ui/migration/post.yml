version: tesseraql/v1
id: tql.studio.migration.create
kind: route
recipe: query-html

input:
  _csrf:
    type: string
    required: false
  datasource:
    type: string
    required: false
  vendor:
    type: string
    required: false
  kind:
    type: string
    required: false
  description:
    type: string
    required: true
  ddl:
    type: string
    required: false

sources:
  main:
    service:
      name: studio.shell.studio.migration.create
      params:
        member: path.member
        shellPermissions: principal.permissions
        shellCookie: header.Cookie
        shellCsrf: params._csrf
        shellCsrfHeader: header.X-CSRF-Token
        datasource: params.datasource
        vendor: params.vendor
        kind: params.kind
        description: params.description
        ddl: params.ddl
        actor: principal.loginId
        permissions: principal.permissions
response:
  html:
    status: 200
    template: created.html
    model:
      mig: main
