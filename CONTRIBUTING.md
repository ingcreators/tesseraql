# Contributing

## Local development

Use the Dev Container.

```bash
mvn -B -ntp verify
```

## Java

TesseraQL 1.x uses Java 21 as the build baseline and Java 25 as a compatibility target.

## Commit scope

Keep generated artifacts out of source commits unless the change is explicitly about generated artifact reproducibility.
