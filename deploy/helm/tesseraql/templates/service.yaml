apiVersion: v1
kind: Service
metadata:
  name: {{ include "tesseraql.fullname" . }}
  labels:
    {{- include "tesseraql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "tesseraql.selectorLabels" . | nindent 4 }}
